spring:
  application:
    name: charity-backend
  datasource:
    url: jdbc:mysql://localhost:3306/charity?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Tehran&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

server:
  port: 8082

jwt:
  secret: ${JWT_SECRET:changeThisSecretKeyToAVeryLongRandomStringAtLeast256BitsLongForProduction}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN:REPLACE_WITH_YOUR_BOT_TOKEN}
  channel: ${TELEGRAM_CHANNEL:@your_channel_username}
  enabled: ${TELEGRAM_ENABLED:false}

cors:
  allowed-origins: ${CORS_ORIGINS:http://127.0.0.1:5173}

file:
  upload-dir: ${FILE_UPLOAD_DIR:./uploads}
